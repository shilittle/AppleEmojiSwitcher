# 更新记录

## v1.2.0 · 预览版

- 新增极简 CLI：中文数字菜单及 install、restore、cancel、status、verify、help 命令。
- 直接安装固定苹果字体，不包含或下载 Python、字体构建器、Unicode 数据或渲染组件。
- GUI 和 CLI 共用固定字体校验、缓存互斥锁、备份、队列与回退逻辑。
- 新增极简字体专用参数集和模式识别；兼容旧事务，禁止重复安装时自动切换模式。
- CLI 提权结果按调用独立保存，返回具体错误及可用于脚本的退出码。
- 提供独立 CLI 便携包及 SHA-256；CLI 的 verify 只验证文件和事务状态。
- 修复严格错误处理下对已删除启动收尾任务的误报；暂存字体按最初批准的哈希再次校验。

## v1.1.1 · 预览版

首个公开版本，当前发布为 prerelease。已验证真实重启替换、权限恢复及系统字体渲染；应用人工核对和完整恢复原生流程仍待验证。

- 提供 Windows 10/11 x64 中文可视化便携工具，支持 Apple Emoji 替换、取消待重启操作和恢复原生字体。
- 固定 Apple 字体、Python、构建依赖和 Unicode 17.0 数据，并按 SHA-256 校验；缓存不修改全局 Python、PATH 或系统代理。
- 构建前检查完整 Emoji 字符串，并补齐 Apple 缺少而 Windows 原生支持的项目。
- 允许局部显示差异继续替换；英格兰、苏格兰和威尔士旗的本机黑旗显示会记录为警告。
- 修复 Windows 重启队列中带 `*1` 前缀的路径识别、取消和核验。
- 使用显式可写的注册表句柄处理队列清理，不修改注册表权限。
- 管理员阶段失败时透传具体错误，避免只显示笼统的退出码。
- 保留原字体、权限和其他待重启操作，并提供离线恢复路径。
